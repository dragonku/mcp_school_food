name: korean-school-meal-bot
description: 한국 학교 급식 정보 조회 서버

build:
  type: docker

env:
  NEIS_API_KEY:
    description: NEIS Open API 키
    required: true
  PORT:
    description: 서버 포트
    default: "3000"
  TRANSPORT:
    description: 전송 프로토콜
    default: "ws"

ports:
  - "3000:3000" 